{
  "name": "lifeos-practice-tests",
  "version": "1.0.0",
  "description": "Practice Testing App",
  "private": true,
  "scripts": {
    "build": "cd react-app && npm install --legacy-peer-deps && npm run build",
    "start": "cd backend && node server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "googleapis": "^118.0.0"
  }
}
